name: 'ledger_sync'
version: '1.0.0'
config-version: 2

profile: 'test' 

model-paths: ["models"]
clean-targets: ["target", "dbt_packages"]

models:
  ledger_sync:
    marts:
      +materialized: table 